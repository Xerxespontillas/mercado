import 'package:flutter/material.dart';

class Farmers extends StatelessWidget {
  const Farmers({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Farmer'));
  }
}
